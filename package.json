{
  "name": "barktown",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "vite dev",
    "build": "vite build",
    "preview": "npm run build && wrangler dev",
    "preprocess": "node scripts/build-audio-index.mjs",
    "fetch-sun": "node scripts/fetch-sun-data.mjs",
    "deploy": "npm run build && wrangler deploy"
  },
  "devDependencies": {
    "@sveltejs/adapter-cloudflare": "^7.2.6",
    "@sveltejs/kit": "^2.0.0",
    "@sveltejs/vite-plugin-svelte": "^5.0.0",
    "svelte": "^5.0.0",
    "vite": "^6.0.0",
    "wrangler": "^4.63.0"
  },
  "type": "module",
  "dependencies": {
    "d3": "^7.9.0"
  }
}
